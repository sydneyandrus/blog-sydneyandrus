<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, intitial-scale=1" />
        <title>{{ PageTitle }}</title>
        <link rel="stylesheet" href={{ stylesheet | default: "/css/content.css"}}>
    </head>
    <body>
        {{ content }}
    </body>
</html>